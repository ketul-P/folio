import SectionWrapper from './SectionWrapper';
import sectionStyles from './SectionWrapper.module.css';
import sectionLabel from './SectionWrapper.module.css';
import sectionTitle from './SectionWrapper.module.css';
import own from './Summary.module.css';
import { summary } from '../data';

export default function Summary() {
  return (
    <SectionWrapper id="summary">
      
    </SectionWrapper>
  );
}
